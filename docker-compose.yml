version: '3.8'

services:
  antilag-balancer:
    build: .
    container_name: antilag-balancer
    restart: always
    ports:
      - "8080:8080"
      - "1080:1080"
      - "1081:1081"
    environment:
      - NODE_ENV=production
      - PORT=8080
